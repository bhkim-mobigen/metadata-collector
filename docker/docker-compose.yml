version: '0.1'

services:
  metadata-collector:
    build:
      context: ../
      dockerfile: docker/Dockerfile
    image: data-catalog-metadata-collector:$DATA_CATALOG_SOLUTION_IMG_VERSION
    container_name: metadata-collector
    ports:
      - "8587:8587"
    volumes:
      - $DATA_CATALOG_SOLUTION_DATA/metadata-collector/conf:/metadata-collector/conf
      - $DATA_CATALOG_SOLUTION_DATA/metadata-collector/logs:/metadata-collector/logs
      - $DATA_CATALOG_SOLUTION_DATA/ingestion/script:/metadata_manager/ingestion/script
    environment:
      - METADATA_COLLECTOR_CONFIG_FILE=/metadata-collector/conf/config.yaml
    command: ["tail", "-f", "/dev/null"]

